import { LoginScreen } from "@/screens/auth/login";

export const metadata = {
  title: "Đăng Nhập | Tiệm Len Nhà Kiều Admin",
  description: "Cổng đăng nhập hệ thống quản trị Tiệm Len Nhà Kiều",
};

export default function LoginPage() {
  return <LoginScreen />;
}
